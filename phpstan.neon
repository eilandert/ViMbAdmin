includes:
    - phpstan-common.neon
    - phpstan-baseline.neon

parameters:
    # Repository-wide floor. Existing debt is recorded in the baseline; new
    # level-3 errors are blocking.
    level: 3
