includes:
    - phpstan-common.neon
    - phpstan-baseline.neon

parameters:
    # Repository-wide target. Existing debt is recorded in the generated
    # baseline; every diagnostic without an exact baseline match is blocking.
    level: 10
